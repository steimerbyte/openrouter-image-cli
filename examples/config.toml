# Example configuration for openrouter-image.
# Copy to ~/.config/openrouter-image/config.toml and edit.

# Required: your OpenRouter API key (https://openrouter.ai/keys)
api_key = "sk-or-v1-REPLACE_ME"

# Optional: default model if --model is not passed on the CLI.
# Falls back to "bytedance-seed/seedream-4.5" when unset.
# default_model = "bytedance-seed/seedream-4.5"
